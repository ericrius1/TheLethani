{
  "name": "the-way",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "assets:publish": "node scripts/publish-r2-media.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "prepare": "node scripts/install-hooks.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.14",
    "wrangler": "^4.94.0"
  }
}
